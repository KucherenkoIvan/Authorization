# Use postgres/example user/password credentials
version: '3.1'

services:
  auth-db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: A1s2d34F
      POSTGRES_DB: authDB
    ports:
      - 5432:5432

  auth-db-adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080